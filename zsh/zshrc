# =========================
# Main zshrc - Modular Configuration Loader
# =========================
# This file loads all zsh configuration modules from zsh.d/ in numerical order.
#
# Module organization:
#   00-init.zsh         - Powerlevel10k instant prompt, OS detection, OS-specific setup
#   10-plugins.zsh      - Completions, plugin loading, prompt configuration
#   20-env.zsh          - Environment variables, workspace paths, history settings
#   30-tools.zsh        - Modern CLI tools (eza, fzf, dust, yazi, etc.)
#   40-aliases.zsh      - Navigation, bitwarden, dotfiles, and utility aliases
#   50-functions.zsh    - Shell functions (status, j, notes, dotfiles-upgrade, ssh)
#   60-aws.zsh          - AWS profile management and SSO helpers
#   70-claude.zsh       - Claude Code wrapper and routing logic
#   80-git.zsh          - Git shortcuts and clipboard utilities
#   90-integrations.zsh - Tool integrations (SDKMAN, NVM, zoxide, glow, syntax highlighting)
#
# To customize: Edit individual module files in ~/workspace/dotfiles/zsh/zsh.d/
# For machine-specific overrides: Create 99-local.zsh (see 99-local.zsh.example)

# Load all configuration modules in order
# Use ${0:A:h} to get the real directory of this file (following symlinks)
ZSHRC_DIR="${0:A:h}"
for config_file in "$ZSHRC_DIR"/zsh.d/*.zsh(N); do
  source "$config_file"
done
